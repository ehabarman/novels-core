extend type Query {
    user(id: String!): User!
}

type User {
    id: String!
    username: String!
    email: String!
    createdAt: String!
    writtenBooks(pagination: NumericPaginationInput): [Book!]!
}